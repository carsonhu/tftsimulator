# Runtime deps for `streamlit run app.py` -- the local dev launch (run_app.bat)
# and, through stlite.html, the Mana Generation page. The static build at the
# site root needs none of this: it runs the simulator under bare Pyodide.
#
# Floors, not pins, except where a pin is load-bearing. Test/analysis-only
# packages live in requirements-dev.txt.

streamlit>=1.40
pandas>=2.2
numpy>=1.26
XlsxWriter>=3.2
# Pinned deliberately: stlite.html pins the same version, and the two must move
# together or a local run and the browser render with different plotly. 6.x
# rather than 5.x is load-bearing there -- the 5.x wheel was ~15,300 files that
# Pyodide unpacked on every visit, against ~1,660 for 6.x. (The static root
# build doesn't use this package at all; it loads plotly.js from a CDN, pinned
# to the version this one bundles.)
plotly==6.9.0
